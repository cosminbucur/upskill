server.port: 8761

spring:
  application:
    name: eureka-server

eureka:
  # prevent eureka register with self (stop acting like a client)
  client:
    register-with-eureka: false
    fetch-registry: false
    server:
      waitTimeInMsWhenSyncEmpty: 0